<UserControl x:Class="StockMate.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockMate.WPF.Views"
             xmlns:controls="clr-namespace:StockMate.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Home" Grid.Row="0" />

        <!-- Using StackPanel to align CryptoCards controls vertically -->
        <StackPanel Grid.Row="1" Margin="0,10" VerticalAlignment="Top">
            <controls:CryptoCards DataContext="{Binding CryptoAssetViewModel.Bitcoin}" Margin="0,10"/>
            <controls:CryptoCards DataContext="{Binding CryptoAssetViewModel.Ethereum}" Margin="0,10"/>
        </StackPanel>
    </Grid>
</UserControl>
